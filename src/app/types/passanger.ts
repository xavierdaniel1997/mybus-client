export interface Passenger {
  name: string;
  age: number;
  gender: string;
}

export interface ContactDetails {
  phoneCode: string;
  phone: string;
  email: string;
  state: string;
  whatsappEnabled: boolean;
}